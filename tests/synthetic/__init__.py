"""Synthesized inputs: exports built in code, never copied from real history.

This repository is public and the data it processes is private engineering history (CLAUDE.md), so
every conversation written here is invented. It is neither a fake — nothing here stands in for a
boundary — nor a contract; it builds the bytes a real implementation is pointed at.
"""
